import 'package:flutter/material.dart';

class FollowUpsView extends StatelessWidget {
  const FollowUpsView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.yellow,
    );
  }
}
